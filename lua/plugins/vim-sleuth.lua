-- vim-sleuth
--
-- Automatically adjust 'shiftwidth' and 'expandtab' heuristically based
-- on the current file, or, in the case the current file is new, blank, or otherwise
-- insufficient, by looking at other files of the same type in the current and
-- parent directories.
-- Also with editorconfig support.
--
-- @link https://github.com/tpope/vim-sleuth

return {
  'tpope/vim-sleuth',
}
